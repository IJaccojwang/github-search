export class User {
  constructor( public avatarimg:string, public user:string, public followers: number, public following: number, public repoNo: number, public gistNo: number, public date: any) {
  }
}
